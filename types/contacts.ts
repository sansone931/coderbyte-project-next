export type Contact = {
  id: number;
  firstName: string;
  lastName: string;
  phoneNumber: string;
};

export type ContactFormData = {
  id: number | null;
  firstName: string;
  lastName: string;
  phoneNumber: string;
};
